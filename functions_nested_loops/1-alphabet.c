#include <stdio.h>
#include "main.c"

/**
 * main - check the code
 *
 * Return: Always 0.
 */

int print_alphabet();
{
	char l;

	for (l = 'a'; l <= 'z'; l++)
	putchar(l);
	putchar('\n');
	return (0);
}
