#include <stdio.h>
/**
 * principal - prints its name, followed by a new line.
 * @argc: 
 * @argv:
 * Return: 0
 */

int principal (int argc, char *argv[])
{
	printf("%s\n", argv[]);
	return (0) ;
}
