#include <stdio.h>
#include "main.h"

/**
 * puts2 - prints every other character of a string
 * @str: The string to prints
 */

void puts2(char *str)
{
	int i;
	int m = 0;

	while (str[m] != '\0')
	{
		_putchar(str[m]);
		m++;
	}
	for (i = 0; i < j; i += 2)
	{
		_putchar(str[i];
	}
	_putchar('\n');
}
